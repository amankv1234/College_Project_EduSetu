<%- include("partials/navbar") %>

<style>
:root{
  --bg1:#0f1724; --bg2:#042b2b; --card:#071827;
  --accent:#4bd1c6; --accent2:#6b5cff; --muted:#9aa4b2;
}
*{box-sizing:border-box}
body{
  margin:0; font-family:Inter,system-ui,sans-serif;
  background:linear-gradient(180deg,var(--bg2),var(--bg1));
  color:#e6eef8;
}
.container{max-width:1400px;margin:auto;padding:40px 20px 100px}
.hero{text-align:center;margin-bottom:40px}
.hero h1{font-size:36px;margin-bottom:8px}
.hero p{color:var(--muted);max-width:700px;margin:auto}

.dashboard{display:grid;grid-template-columns:320px 1fr;gap:24px}
@media(max-width:1000px){.dashboard{grid-template-columns:1fr}}

.card{
  background:linear-gradient(180deg,rgba(255,255,255,.04),rgba(255,255,255,.01));
  border-radius:18px;padding:22px;
  box-shadow:0 12px 35px rgba(0,0,0,.55)
}
.card h3{margin-top:0}

/* LEFT PANEL */
.skill{margin-bottom:24px}
.skill label{display:flex;justify-content:space-between;color:var(--muted);font-size:14px}
.progress{height:16px;background:#1a2536;border-radius:999px;overflow:hidden;margin-top:6px}
.progress-fill{height:100%;background:linear-gradient(90deg,var(--accent),var(--accent2));width:0%;font-size:11px;text-align:right;padding-right:6px;line-height:16px}
input[type=range]{width:100%;margin-top:10px}

.btn{
  width:100%;padding:14px;margin-top:10px;border:none;border-radius:14px;
  background:linear-gradient(90deg,var(--accent),var(--accent2));
  font-size:16px;font-weight:600;cursor:pointer
}

/* RIGHT PANEL */
.result-box{display:none;background:#020c16;border-radius:14px;padding:18px;font-family:monospace;margin-top:10px}
.badge{display:inline-block;padding:6px 14px;border-radius:999px;background:rgba(75,209,198,.15);margin-bottom:10px}
.score{font-size:42px;font-weight:700;margin:10px 0}
.meter{height:22px;background:#111;border-radius:999px;overflow:hidden}
.meter-fill{height:100%;background:linear-gradient(90deg,#22c55e,#16a34a);width:0%}

.cards-mini{display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:16px;margin-top:20px}
.mini{background:#061627;padding:16px;border-radius:14px}
.mini h4{margin:0 0 6px}

.timeline{margin-top:20px;padding-left:18px}
.timeline li{margin-bottom:10px;color:var(--muted)}

.footer-note{text-align:center;margin-top:50px;color:var(--muted)}
</style>

<div class="container">
  <div class="hero">
    <h1>🚀 Placement Success Dashboard</h1>
    <p>Visualize your readiness, confidence level and placement probability with an AI-style experience</p>
  </div>

  <div class="dashboard">
    <!-- INPUT PANEL -->
    <div class="card">
      <h3>📊 Skill Progress Input</h3>

```
  <div class="skill">
    <label>DSA <span id="dsaVal">40%</span></label>
    <div class="progress"><div class="progress-fill" id="dsaFill"></div></div>
    <input type="range" id="dsa" min="0" max="100" value="40" oninput="update('dsa')">
  </div>

  <div class="skill">
    <label>Web Development <span id="webVal">30%</span></label>
    <div class="progress"><div class="progress-fill" id="webFill"></div></div>
    <input type="range" id="web" min="0" max="100" value="30" oninput="update('web')">
  </div>

  <div class="skill">
    <label>Communication <span id="engVal">50%</span></label>
    <div class="progress"><div class="progress-fill" id="engFill"></div></div>
    <input type="range" id="eng" min="0" max="100" value="50" oninput="update('eng')">
  </div>

  <button class="btn" onclick="predict()">🔮 Analyze Placement Readiness</button>
</div>

<!-- OUTPUT PANEL -->
<div class="card">
  <span class="badge">AI Result</span>
  <div class="score" id="score">--</div>
  <div class="meter"><div class="meter-fill" id="meter"></div></div>

  <div id="result" class="result-box"></div>

  <div class="cards-mini">
    <div class="mini"><h4>📚 DSA Focus</h4><p id="dsaTip">-</p></div>
    <div class="mini"><h4>🌐 Web Projects</h4><p id="webTip">-</p></div>
    <div class="mini"><h4>🗣️ Communication</h4><p id="engTip">-</p></div>
  </div>

  <h4 style="margin-top:24px">🧠 Smart Improvement Plan</h4>
  <ul class="timeline" id="plan"></ul>
</div>
```

  </div>

  <div class="footer-note">EduSetu • AI-inspired frontend simulation • Backend can be plugged later</div>
</div>

<script>
function update(id){
  const v=document.getElementById(id).value;
  document.getElementById(id+'Fill').style.width=v+'%';
  document.getElementById(id+'Fill').innerText=v+'%';
  document.getElementById(id+'Val').innerText=v+'%';
}
['dsa','web','eng'].forEach(update);

function predict(){
  const d=+dsa.value, w=+web.value, e=+eng.value;
  const score=Math.round(d*0.4+w*0.35+e*0.25);
  document.getElementById('score').innerText=score+'/100';
  document.getElementById('meter').style.width=score+'%';
  result.style.display='block';

  let msg='';
  if(score>=80) msg='🔥 Excellent! You are almost placement ready.';
  else if(score>=60) msg='👍 Good progress. Strengthen weak areas.';
  else msg='🚀 Focus more on fundamentals and projects.';
  result.innerText=msg;

  dsaTip.innerText=d<60?'Practice daily problems':'Maintain consistency';
  webTip.innerText=w<60?'Build 2 real projects':'Polish advanced concepts';
  engTip.innerText=e<60?'Speak & write daily':'Mock interviews';

  plan.innerHTML=`
    <li>Daily 1–2 hours DSA practice</li>
    <li>Weekly project or feature build</li>
    <li>English speaking & mock interviews</li>
    <li>Monthly resume + GitHub review</li>`;
}
</script>
